#include "main.h"
#include <ctype.h>

int isupper(int c);
{
	char ch;

	/*function that checks for uppercase character*/
        printf("c:":
	scanf("%c", &ch);		
    
        if(isupper(ch))
        {
            Return 1 if c is uppercase
        }
        else if(islower(ch));
	{
	     Return 0 if otherwise
	}     
	    return (0);
}
